# Copy this file to .env.local and fill in the values for local development.
# .env.local is gitignored — never commit real keys.

# Firebase Web API Key (public identifier — safe to embed in client code)
# Get from: Firebase Console → Project Settings → Your apps → Web app → apiKey
VITE_FIREBASE_API_KEY=

# Gemini AI API Key
# Get from: https://aistudio.google.com/app/apikey
# In production (GitHub Actions build): stored as GitHub secret GEMINI_API_KEY
# In production (App Hosting build):    stored as Secret Manager secret GEMINI_API_KEY
# In staging:                           stored as Secret Manager secret GEMINI_API_KEY_STAGING
VITE_GEMINI_API_KEY=

# ---------------------------------------------------------------------------
# Multi-environment App Hosting configuration
# ---------------------------------------------------------------------------
# Production backend:
#   Firebase Console → App Hosting → <production backend> → Settings → Environment
#   Leave "Environment name" blank (uses apphosting.yaml directly)
#   Resources: 2 CPU / 1024 MiB / minInstances=1 / maxInstances=4
#
# Staging backend:
#   Firebase Console → App Hosting → <staging backend> → Settings → Environment
#   Set "Environment name" to: staging  (uses apphosting.staging.yaml overrides)
#   Resources: 1 CPU / 512 MiB / minInstances=0 / maxInstances=2
# ---------------------------------------------------------------------------
